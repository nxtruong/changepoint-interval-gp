function M = samplingmetric_maxvartrain(s2train, ~, ~)
%SAMPLINGMETRIC_MAXVARTRAIN Sample selection metric variance.
%   The sample selection metric is just the training variance.
%     metric = variance
M = s2train;
end

